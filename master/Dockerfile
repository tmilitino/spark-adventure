FROM image_base

COPY ./entrypoint.sh /master/entrypoint.sh

ENV SPARK_MASTER_PORT 7077
ENV SPARK_MASTER_WEBUI_PORT 8080
ENV SPARK_MASTER_LOG /spark/logs
ENV SPARK_EXECUTOR_MEMORY 512m

EXPOSE 80

WORKDIR /files